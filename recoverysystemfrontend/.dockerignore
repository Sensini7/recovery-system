# Exclude node_modules to avoid copying local dependencies
node_modules
# If using Yarn Plug'n'Play
.pnp
.pnp.js

# Exclude build output; Docker will generate its own build
build

# Exclude test coverage reports
coverage

# Exclude Git files to prevent copying version history
.git
.gitignore

# Exclude environment variable files for security
.env
.env.local
.env.development.local
.env.test.local
.env.production.local

# Exclude log files
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# Exclude OS-specific files
.DS_Store
Thumbs.db

# Exclude editor and IDE specific files
.vscode
.idea
*.sublime-project
*.sublime-workspace

# Exclude Docker-specific files if not needed inside the container
Dockerfile
docker-compose.yml

# Exclude any other files or directories you don't need in the image
*.md
*.log
